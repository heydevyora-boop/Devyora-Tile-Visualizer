# Never commit real environment values — only *.env.example files should be tracked.
.env
.env.local
.env.*.local

node_modules
dist
*.log

.DS_Store

# Runtime generation history (contains customer tile photos and generated
# images). Created on first save; never commit it.
server/data/
